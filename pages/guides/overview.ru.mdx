---
showFooter: false
---
import { Tab, Tabs } from 'nextra-theme-docs';
import Card, { Cards } from '@components/core/Card';
import cicdGuides from './ci-cd/_meta.ru.json';
import konteynerGuides from './konteyner/_meta.ru.json';
import k8sGuides from './k8s/_meta.ru.json';
import databaseGuides from './database/_meta.ru.json';
import webserverGuides from './web-server/_meta.ru.json';
import cloudGuides from './cloud/_meta.ru.json';
import monitoringGuides from './monitoring/_meta.ru.json';

# Руководства

Документация и руководства от сообщества. Темы включают **CI/CD, Контейнеризацию, Kubernetes(k8s), Мониторинг, Сети, Load Balancing** и многое другое.

<Tabs items={['▶️CI/CD','▶️Контейнеры','▶️Kubernetes','▶️Базы данных','▶️Веб серверы','▶️Облако','▶️Мониторинг']}>
  <Tab style={{ paddingTop: 8 }}>
    <Cards
      num={3}
      children={Object.keys(cicdGuides).map((key, i) => (
        <Card
          key={i}
          title={cicdGuides[key].title}
          description={cicdGuides[key].description}
          icon={cicdGuides[key].icon}
          href={cicdGuides[key].href}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(konteynerGuides).map((key, i) => (
        <Card
          key={i}
          title={konteynerGuides[key].title}
          description={konteynerGuides[key].description}
          icon={konteynerGuides[key].icon}
          href={konteynerGuides[key].href}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(k8sGuides).map((key, i) => (
        <Card
          key={i}
          title={k8sGuides[key].title}
          description={k8sGuides[key].description}
          icon={k8sGuides[key].icon}
          href={k8sGuides[key].href}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(databaseGuides).map((key, i) => (
        <Card
          key={i}
          title={databaseGuides[key].title}
          description={databaseGuides[key].description}
          icon={databaseGuides[key].icon}
          href={databaseGuides[key].href}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(webserverGuides).map((key, i) => (
        <Card
          key={i}
          title={webserverGuides[key].title}
          description={webserverGuides[key].description}
          icon={webserverGuides[key].icon}
          href={webserverGuides[key].href}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(cloudGuides).map((key, i) => (
        <Card
          key={i}
          title={cloudGuides[key].title}
          description={cloudGuides[key].description}
          icon={cloudGuides[key].icon}
          href={cloudGuides[key].href}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(monitoringGuides).map((key, i) => (
        <Card
          key={i}
          title={monitoringGuides[key].title}
          description={monitoringGuides[key].description}
          icon={monitoringGuides[key].icon}
          href={monitoringGuides[key].href}
        />
      ))}
    />
  </Tab>
</Tabs>
